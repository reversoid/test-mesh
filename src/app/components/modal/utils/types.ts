export interface IErrors {
  name: string | null;
  description: string | null;
  price: string | null;
}

export interface ProductFormType {
  name: string;
  description: string;
  price: string;
}
